version: '3.8'

services:
  sentry-feishu-webhook:
    build: .
    container_name: sentry-feishu-webhook
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"